// 1 - O sistema deve usar o tailwindcss
// 2 - o sistema deve ter um elemento html do tipo table (com id definido) preparado sem informações dentro
// 3 - São necessários dois arrays para gerar a tabela...
		// 3.1 - Um array de dados
		// 3.2 - um array com objetos que caracterizão as colunas
		// 3.3 - função de formatação opcinal




type columnObject = {

	columnLabel: string;
	accessor : string;
	formatFN? : (info: string | number) => string;
};

type columnsArray = columnObject[];

